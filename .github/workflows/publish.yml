# name: Publish Docker image
# on:
#   release:
#     types: [published]
# jobs:
#   push_to_registry:
#     name: Push Docker image to GitHub Packages
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out the repo
#         uses: actions/checkout@v2
#       - name: Push to GitHub Packages
#         uses: docker/build-push-action@v1
#         with:
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#           registry: docker.pkg.github.com
#           repository: FoxBandyKoot/TP_CI_CD
#           tag_with_ref: true

name: publish

on: 
  push:
    branches:
      -main

jobs:
  publish-hello-world-image:
    runs-on: ubuntu-latest

    steps:

    #- uses: login-action@v1
    - uses: actions/checkout@v2
      #- name: Build the tp_ci_cd Docker image

      with:
        repo-token: ${{ secrets.CR_PAT }}
#             registry: ghcr.io
            
#             username: ${{ github.repository_owner }}

        run: |
          echo $CR_PAT | docker login ghcr.io -u FoxBandyKoot --password-stdin
          docker build . --tag ghcr.io/foxbandykoot/tp_ci_cd:latest
          docker run ghcr.io/foxbandykoot/tp_ci_cd:latest
          docker push ghcr.io/foxbandykoot/tp_ci_cd:latest
    
    




